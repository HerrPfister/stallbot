{
  "name": "stallbot",
  "version": "1.0.0",
  "description": "A simple bot that will notify if any stalls in the bathrooms are occupied",
  "main": "index.js",
  "scripts": {
    "shell": "hubot --adapter shell --disable-httpd",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "hubot",
    "hubot-scripts"
  ],
  "devDependencies": {
    "coffee-script": "^1.10.0",
    "hubot": "^2.17.0"
  },
  "dependencies": {
    "lodash": "^3.10.1",
    "request": "^2.67.0"
  }
}
